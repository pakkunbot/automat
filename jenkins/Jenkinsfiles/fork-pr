pipeline {
    agent {
        label 'docker'
    }
    environment {
        PATH = "/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin"
    }
    stages {
        stage('DEBUG') {
            agent {
                docker {
                    image 'cliffano/studio:latest'
                    args '--user root --network host'
                }
            }
            steps {
                echo 'Hellooooo'
                echo "$PATH"
            }
        }
    }
}
