pipeline {
    agent {
        label 'docker'
    }
    stages {
        stage('DEBUG') {
            agent {
                docker {
                    image 'cliffano/studio:latest'
                    args '--user root'
                }
            }
            steps {
                echo 'Hellooooo'
                echo "$PATH"
            }
        }
    }
}
